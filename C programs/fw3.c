#include<stdio.h>
void main()
{
	int a[50],i,n;
	printf("Enter total number of elements:");
	scanf("%d",&n);
	printf("Enter array elements:");
	for(i=a;i<n;i++)
	{
		if(a[i]%2==0)
		{
			printf("%d is odd",&a[])
		}
	}
}
